
const Button = ({name}) => {
  return (
    <div>
        <button className="bg-blue-500 text-white font-bold rounded py-2 px-4 justify-center">{name}</button>
    </div>
  )
}

export default Button